(()=>{'use strict';const $=id=>document.getElementById(id),cfg=window.RESERVAS_CONFIG||{};let client,context,quantities={};function msg(t,ok=false){$('message').textContent=t;$('message').classList.toggle('ok',ok);$('message').hidden=false}function clean(v){return String(v).replace(/\D/g,'')}function render(filter=''){const list=(context.equipment||[]).filter(x=>x.name.toLowerCase().includes(filter.toLowerCase()));$('equipmentList').innerHTML=list.map(x=>`<div class="equipment"><div><strong>${x.name}</strong><small>${x.available} disponibles</small></div><div class="quantity"><button type="button" data-id="${x.id}" data-delta="-1" aria-label="Quitar ${x.name}">−</button><output id="qty-${x.id}">${quantities[x.id]||0}</output><button type="button" data-id="${x.id}" data-delta="1" aria-label="Agregar ${x.name}">+</button></div></div>`).join('')||'<p style="padding:16px">No se encontraron equipos.</p>'}async function identify(e){e.preventDefault();$('message').hidden=true;const id=clean($('nationalId').value);const{data,error}=await client.rpc('student_loan_context',{p_national_id:id});if(error)return msg(error.message);if(!data?.found)return msg('No encontramos un estudiante registrado con esa cédula.');if(!data.authorized)return msg('Todavía no existe una autorización docente vigente para solicitar equipos.');context={...data,nationalId:id};quantities={};$('studentName').textContent=data.full_name;$('authorization').textContent=`Autorizado: ${data.authorization_label}`;$('requestArea').hidden=false;render();const d=new Date(Date.now()+86400000);d.setMinutes(d.getMinutes()-d.getTimezoneOffset());$('returnAt').value=d.toISOString().slice(0,16);$('requestArea').scrollIntoView({behavior:'smooth'})}async function send(e){e.preventDefault();const items=Object.entries(quantities).filter(([,q])=>q>0).map(([id,quantity])=>({id:Number(id),quantity}));if(!items.length)return msg('Seleccione al menos un equipo.');const{data,error}=await client.rpc('create_student_loan_request',{p_national_id:context.nationalId,p_authorization_id:context.authorization_id,p_expected_return_at:new Date($('returnAt').value).toISOString(),p_items:items});if(error)return msg(error.message);$('requestNumber').textContent=data.request_number;$('confirmation').showModal()}$('idForm').addEventListener('submit',identify);$('requestForm').addEventListener('submit',send);$('search').addEventListener('input',e=>render(e.target.value));$('equipmentList').addEventListener('click',e=>{const b=e.target.closest('[data-id]');if(!b)return;const id=b.dataset.id,item=context.equipment.find(x=>String(x.id)===id);quantities[id]=Math.max(0,Math.min(item.available,(quantities[id]||0)+Number(b.dataset.delta)));render($('search').value)});$('newRequest').addEventListener('click',()=>location.reload());if(!cfg.supabaseUrl||!cfg.supabaseAnonKey||!window.supabase?.createClient)msg('El servicio no está disponible.');else client=window.supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey);})();
